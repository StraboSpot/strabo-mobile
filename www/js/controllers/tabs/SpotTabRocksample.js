angular
  .module('app')
  .controller('SpotTabRocksampleController', function ($scope,
                                                 $log) {
    $log.log('inside spot tab rock sample Controller');
  });
